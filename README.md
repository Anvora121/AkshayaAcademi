# Akshaya Akademics

Welcome to **Akshaya Akademics** — Your Gateway to World-Class Universities.

This project is built with modern front-end technologies to deliver a fast and seamless user experience:
- [Vite](https://vitejs.dev/) - Next Generation Frontend Tooling
- [React](https://reactjs.org/) (v18) - A JavaScript library for building user interfaces
- [TypeScript](https://www.typescriptlang.org/) - Typed JavaScript at Any Scale
- [Tailwind CSS](https://tailwindcss.com/) - Rapidly build modern websites without ever leaving your HTML
- [shadcn/ui](https://ui.shadcn.com/) - Beautifully designed components built with Radix UI and Tailwind CSS

## 🚀 Getting Started

Follow these steps to get the project up and running locally:

### 1. Requirements

Ensure you have [Node.js](https://nodejs.org/) installed completely.

### 2. Installation

Clone the repository if you haven't already, then navigate into the project directory and install the dependencies:

```bash
npm install
```

### 3. Running the Development Server

Start the development server with auto-reloading:

```bash
npm run dev
```

The application will typically be available at `http://localhost:5173`. Open this URL in your browser to view the app!

## 🛠️ Scripts Overview

- `npm run dev` - Starts the Vite development server.
- `npm run build` - Compiles the application for production use into the `dist` folder.
- `npm run build:dev` - Compiles the application for development use.
- `npm run lint` - Runs ESLint to find and optionally fix problems in your code.
- `npm run test` - Runs Vitest test suites.
- `npm run preview` - Boots up a local static web server that serves the files from the `dist` folder (after `npm run build`).

## 📁 Project Structure

- `/src` - Contains all React components, hooks, utilities, pages, and application logic.
- `/public` - Houses static assets that are served directly.
- `/backend` - Contains the backend server implementation.

## 🤝 Contributing

When contributing to this repository, please first discuss the change you wish to make via issue, email, or any other method with the owners of this repository before making a change.
